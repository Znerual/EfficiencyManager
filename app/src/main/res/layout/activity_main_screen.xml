<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#FFFFFF"
    tools:context="laurenzsoft.com.efficiencymanager.MainScreen"
    android:orientation="vertical">

    <ImageButton
        android:src="@drawable/work"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:background="#FFFFFF"
        android:text="@string/work"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="0.2"
        android:id="@+id/mainWorkButton" />

    <ImageButton
        android:src="@drawable/leisuretime"
        android:scaleType="fitCenter"
        android:background="#FFFFFF"
        android:text="@string/leisureTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="0.2"
        android:id="@+id/mainLeisureButton"
        android:adjustViewBounds="true"
        android:cropToPadding="false" />

    <ImageButton
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/stats"
        android:background="#FFFFFF"
        android:id="@+id/mainStatsButton"
        android:layout_weight="0.6"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:cropToPadding="true" />
</LinearLayout>
